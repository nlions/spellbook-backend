package de.geilepaste.spellbookbackend.model.dao;

import javax.persistence.Entity;
import javax.persistence.Table;

//TODO not sure if this is needed or it could just be an enum. Do we want custom classes?
@Entity
@Table(name = "class")
public class Class {
}
